{
  "name": "construction-journal",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "setup": "pnpm --filter @construction-journal/shared build && pnpm --filter api exec prisma migrate deploy && pnpm --filter api exec prisma db seed"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  }
}
